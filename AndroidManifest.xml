<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="deepble.navigation"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/sail"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="deepble.navigation.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="deepble.navigation.DeviceScanActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="deepble.navigation.DeviceContextActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="deepble.navigation.AnchorActivity"
            android:label="@string/title_activity_anchor" >
        </activity>
        <activity
            android:name="deepble.navigation.BluetoothChat"
            android:label="@string/title_activity_bluetooth_chat" >
        </activity>
        <activity
            android:name="deepble.navigation.DeviceListActivity"
            android:label="@string/title_activity_device_list" >
        </activity>
    </application>

</manifest>
